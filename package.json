{
  "name": "@papercusp/embedded-pg-discovery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic Postgres connection-URL discovery for desktop / local-first apps: env vars → JSON discovery file → fallback. Pure, zero domain coupling, parameterized.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
